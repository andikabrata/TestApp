<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TestAppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApp"
        tools:targetApi="31">
        <activity
            android:name=".persentation.feature.splash_screen.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.TestApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".persentation.feature.sample.SampleActivity"
            android:exported="false" />
        <activity
            android:name=".persentation.feature.home_news.HomeNewsActivity"
            android:exported="false" />
        <activity
            android:name=".persentation.feature.detail_news.DetailNewsActivity"
            android:exported="false" />
        <activity
            android:name=".persentation.feature.notification.NotificationActivity"
            android:exported="false" />
        <activity
            android:name=".persentation.feature.search.SearchNewsActivity"
            android:exported="false" />
        <activity
            android:name=".persentation.feature.home_rcti.HomeRctiActivity"
            android:exported="false" />


    </application>

</manifest>